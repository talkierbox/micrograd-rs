# Micrograd-RS

Implementing an MLP engine + forward and backward pass in rust. Also included some utilities for logging. 

## Why does this exist?

Mainly to just learn rust and get more experience with the algorithmic side of ML libraries. 